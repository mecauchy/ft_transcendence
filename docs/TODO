-display the pending friend request on the sender's network tab
-fix achievements STILL NOT WORKING
-fix levelup still not notifying
-notifications delete all doesnt work (XHRDELETE
https://localhost:8443/api/users/notifications/all
[HTTP/1.1 404 Not Found 6ms]

Uncaught (in promise) 
Object { statusCode: 404, error: "Not Found", message: "Notification not found" })

-notification bubble for the number of notifs doesnt update properly (when receiving a notif or when clicking a notif to go to the tab)
-make sure everything the user sees in the UI is translatable and follows the i18n
-change the data exported by the export/import to be relevant (so ignore sessions and scenarios, but put the logs data for the games)
-remove the github oauth button
-maybe add an invite button to the discussion to start a pong match between the two friends ? store that in lb properly to know who went against who
-add button on viewing other person's profile to view THEIR history in detail (just like in the history tab for yourself)
